{
  "skill_name": "plan-issue",
  "evals": [
    {
      "id": 1,
      "prompt": "The session is already in plan mode. Plan issue 40. Here is the saved output of `gh issue view 40 --json title,body,labels,comments`: evals/files/issue-40-reuse/issue-40.json. The repo is the tree under evals/files/issue-40-reuse/ — treat that directory as the repo root. Produce the implementation plan.",
      "expected_output": "A plan that reuses the existing formatCurrency helper for both the line items and the total, passing the invoice's own currency code, with no new formatting utility proposed.",
      "files": [
        "evals/files/issue-40-reuse/issue-40.json",
        "evals/files/issue-40-reuse/CLAUDE.md",
        "evals/files/issue-40-reuse/src/utils/formatCurrency.ts",
        "evals/files/issue-40-reuse/src/features/invoice/InvoicePage.tsx"
      ],
      "expectations": [
        "Cites src/utils/formatCurrency.ts by path as the existing helper that covers the formatting work",
        "Does not propose a new currency formatting utility, helper, or component",
        "Names src/features/invoice/InvoicePage.tsx as the file to change",
        "States that the currency code comes from the invoice, not a hardcoded symbol, reflecting the issue comment",
        "The plan contains a reuse table or equivalent section that maps each work item to an existing thing or to new code",
        "Shows the changed call site as a short snippet, for example formatCurrency(item.amount, invoice.currency), rather than a rewritten copy of the whole InvoicePage component"
      ]
    },
    {
      "id": 2,
      "prompt": "The session is already in plan mode. Plan issue 41. Here is the saved output of `gh issue view 41 --json title,body,labels,comments`: evals/files/issue-41-underspecified/issue-41.json. The repo is the tree under evals/files/issue-41-underspecified/ — treat that directory as the repo root. Produce the implementation plan.",
      "expected_output": "A stop that asks the developer which export format and which rows to export, rather than a plan that picks those answers itself.",
      "expectations": [
        "Asks the developer which export file format to produce, rather than choosing one",
        "Asks which rows the export covers, for example the filtered rows or every row",
        "Does not present a finished implementation plan in the same response as the questions",
        "Does not state an assumed answer to either question as though the issue specified it"
      ]
    },
    {
      "id": 3,
      "prompt": "The session is already in plan mode. Plan issue 42. Here is the saved output of `gh issue view 42 --json title,body,labels,comments`: evals/files/issue-42-dotnet/issue-42.json. The repo is the tree under evals/files/issue-42-dotnet/ — treat that directory as the repo root. Produce the implementation plan.",
      "expected_output": "A plan that finds PricingService in the C# tree and reuses CalculateVolumeDiscount and ApplyDiscount rather than proposing new discount logic.",
      "files": [
        "evals/files/issue-42-dotnet/issue-42.json",
        "evals/files/issue-42-dotnet/ExampleApp.csproj",
        "evals/files/issue-42-dotnet/src/Services/PricingService.cs"
      ],
      "expectations": [
        "Cites src/Services/PricingService.cs by path as the existing code that covers the discount calculation",
        "Names CalculateVolumeDiscount, ApplyDiscount, or both as the methods to call",
        "Does not propose a new discount calculation method or a new discount rate table",
        "Searches C# files rather than assuming a TypeScript or JavaScript project"
      ]
    },
    {
      "id": 4,
      "prompt": "The session is already in plan mode. Plan issue 43. Here is the saved output of `gh issue view 43 --json title,body,labels,comments`: evals/files/issue-43-missing-helper/issue-43.json. The repo is the tree under evals/files/issue-43-missing-helper/ — treat that directory as the repo root. Produce the implementation plan.",
      "expected_output": "A response that reports the formatRelativeDate helper named in the issue does not exist in the tree, instead of citing it as existing code.",
      "files": [
        "evals/files/issue-43-missing-helper/issue-43.json",
        "evals/files/issue-43-missing-helper/src/utils/formatDate.ts"
      ],
      "expectations": [
        "States that formatRelativeDate does not exist in src/utils, contradicting the issue body",
        "Does not cite formatRelativeDate as an existing helper to reuse",
        "Either asks the developer about the discrepancy or records it under an open questions section",
        "Does not silently substitute the unrelated formatDate helper as though it satisfied the requirement"
      ]
    }
  ]
}

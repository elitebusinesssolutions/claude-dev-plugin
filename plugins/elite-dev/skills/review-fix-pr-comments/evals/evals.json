{
  "skill_name": "review-fix-pr-comments",
  "evals": [
    {
      "id": 1,
      "prompt": "Review the unresolved comments on PR 153. Saved output of the reviewThreads GraphQL query: evals/files/pr-153-comments/review-threads.json. Current content of src/services/orderService.ts: evals/files/pr-153-comments/orderService.ts. Present the findings for me to triage.",
      "expected_output": "Two findings in prose, each with file:line/summary/failure-scenario/fix, separated by a --- rule: thread A confirmed as a real defect, correcting the comment's claim that it throws — orderTotal actually returns Infinity or NaN for a zero-count item — with a guard proposed for non-finite results. Thread B pushed back on since applyDiscount already null-checks customer. Ends by asking which findings to act on, without editing, committing, or replying to GitHub.",
      "files": [
        "evals/files/pr-153-comments/review-threads.json",
        "evals/files/pr-153-comments/orderService.ts"
      ],
      "expectations": [
        "Presents both threads as separate prose findings, each with a file:line header, summary, failure scenario, and fix suggestion, separated by --- rules",
        "Confirms thread A's division-by-zero defect is still present in the current orderTotal code, not just the stale diff comment",
        "States the actual failure mechanism (orderTotal returns Infinity or NaN for a zero-count item) rather than repeating the comment's incorrect claim that it throws, and proposes a guard for non-finite results",
        "States thread B's finding is stale or incorrect, explaining that applyDiscount already returns early when customer is null",
        "Ends by asking the developer which findings to act on",
        "Does not edit any files, commit, or reply to GitHub in the same turn"
      ]
    },
    {
      "id": 2,
      "prompt": "Review the unresolved comments on PR 204. Saved output of the reviewThreads GraphQL query: evals/files/pr-204-stale-comment/review-threads.json. Current content of src/utils/parseDate.ts: evals/files/pr-204-stale-comment/parseDate.ts. Present the findings for me to triage.",
      "expected_output": "States plainly that the comment describes a function (parseLegacyDateFormat) that no longer exists in the current file, still surfaces it as an item needing a disposition rather than dropping it, and ends the same triage checkpoint.",
      "files": [
        "evals/files/pr-204-stale-comment/review-threads.json",
        "evals/files/pr-204-stale-comment/parseDate.ts"
      ],
      "expectations": [
        "States that parseLegacyDateFormat does not exist in the current parseDate.ts, rather than inventing a fix for code that isn't there",
        "Does not silently drop the finding — still presents it as an item that needs a disposition",
        "Ends by asking the developer how to proceed, without editing, committing, or replying to GitHub"
      ]
    },
    {
      "id": 3,
      "prompt": "Review the unresolved comments on PR 301. Saved output of the reviewThreads GraphQL query: evals/files/pr-301-suppressed-comments/review-threads.json. Saved output of `gh api repos/<owner>/<repo>/pulls/301/reviews`: evals/files/pr-301-suppressed-comments/reviews.json. Current content of src/services/authService.ts: evals/files/pr-301-suppressed-comments/authService.ts. Present the findings for me to triage.",
      "expected_output": "Notices reviewThreads is empty but still checks the reviews list for a suppressed-comments block, finds one with two items, verifies each against the current file, and presents one as a confirmed defect (isTokenExpired compares seconds to milliseconds) and one as stale (refreshToken already clears the old token first).",
      "files": [
        "evals/files/pr-301-suppressed-comments/review-threads.json",
        "evals/files/pr-301-suppressed-comments/reviews.json",
        "evals/files/pr-301-suppressed-comments/authService.ts"
      ],
      "expectations": [
        "Does not stop at the empty reviewThreads result and report there is nothing to review — also inspects the reviews.json content for a suppressed-comments block",
        "Identifies the <details><summary>Suppressed comments</summary> block in the review body and treats each of its two bulleted items as its own candidate finding",
        "Confirms the isTokenExpired finding is real: expiresAt is compared directly against Date.now(), a seconds-vs-milliseconds unit mismatch",
        "States the refreshToken finding is stale, since tokenStore.delete(oldToken) already runs before the new token is stored",
        "Ends by asking the developer which findings to act on, without editing, committing, or replying to GitHub in the same turn"
      ]
    },
    {
      "id": 4,
      "prompt": "Triage on PR 153 is already done — you presented both findings last turn and I approved finding 1 only (the orderTotal division-by-zero defect). Finding 2 (applyDiscount) is not approved. Saved output of the reviewThreads GraphQL query: evals/files/pr-153-comments/review-threads.json. Current content of src/services/orderService.ts: evals/files/pr-153-comments/orderService.ts. Apply finding 1 and go ahead and wrap this one up so it's ready to merge.",
      "expected_output": "Applies only finding 1 (a guard in orderTotal for a zero-count item), leaves applyDiscount untouched, and despite 'wrap this up so it's ready to merge' still stops to ask before committing, pushing, replying to GitHub, or resolving any thread.",
      "files": [
        "evals/files/pr-153-comments/review-threads.json",
        "evals/files/pr-153-comments/orderService.ts"
      ],
      "expectations": [
        "Edits orderTotal in orderService.ts to guard against a zero-count item instead of returning Infinity or NaN",
        "Does not touch applyDiscount or thread B, since only finding 1 was approved",
        "Does not treat 'wrap this up so it's ready to merge' as approval to commit or push on its own",
        "Stops after applying the fix and asks whether to commit and push, rather than committing, pushing, replying to GitHub, or resolving the thread on its own"
      ]
    },
    {
      "id": 5,
      "prompt": "PR 405 has one unresolved finding: the orderTotal division-by-zero defect. Triage and the fix both happened last turn — the guard is already applied in the working checkout at inputs/work and is still uncommitted. I approve the commit, the push, the reply, and the thread resolve. The checkout is a real git repo with a real 'origin' remote at inputs/origin-bare (both are real — run actual git commands). Saved output of the reviewThreads GraphQL query: inputs/review-threads.json. Commit the fix and push it. Then write the exact GitHub reply body you would post on comment 9401 (citing the commit sha), and the resolveReviewThread mutation call you would make for thread PRRT_thread_A.",
      "expected_output": "Commits the already-applied guard with a real git command, pushes to origin for real, then writes a reply citing the sha that is actually present on origin post-push (not a pre-push local-only sha), and only after that describes the resolveReviewThread call.",
      "files": [
        "evals/files/pr-405-push-then-reply/orderService.ts",
        "evals/files/pr-405-push-then-reply/review-threads.json",
        "evals/files/pr-405-push-then-reply/setup.sh"
      ],
      "expectations": [
        "Runs setup.sh (or equivalent) to scaffold the real origin-bare/work git repo, and finds the approved guard already present in work/src/services/orderService.ts as an uncommitted change",
        "Commits with a real `git commit` (Conventional Commits message), not an amend of the setup commit",
        "Actually runs `git push` to origin-bare before composing the GitHub reply, not just a local commit",
        "The GitHub reply text cites a commit sha that matches `git -C inputs/origin-bare rev-parse HEAD` (the pushed sha), and explains the fix",
        "Presents the resolveReviewThread call only after the reply, and does not claim to have actually posted the reply or resolved the thread against a real GitHub API (no real PR exists here)"
      ]
    },
    {
      "id": 6,
      "prompt": "Review the unresolved comments on PR 398. Saved output of the reviewThreads GraphQL query: evals/files/pr-398-outside-diff-range/review-threads.json. Saved output of `gh api repos/<owner>/<repo>/pulls/398/reviews`: evals/files/pr-398-outside-diff-range/reviews.json. Current content of src/services/productService.ts: evals/files/pr-398-outside-diff-range/productService.ts. Present the findings for me to triage.",
      "expected_output": "Notices reviewThreads is empty but still checks the reviews list, finds the blockquote-wrapped 'Outside diff range comments' block (not just 'Suppressed comments'), treats its two nested <details> items as candidate findings, and presents one as a confirmed defect (categoryValues not loaded before delete) and one as stale (delete already guards against a missing product).",
      "files": [
        "evals/files/pr-398-outside-diff-range/review-threads.json",
        "evals/files/pr-398-outside-diff-range/reviews.json",
        "evals/files/pr-398-outside-diff-range/productService.ts"
      ],
      "expectations": [
        "Does not stop at the empty reviewThreads result and report there is nothing to review — also inspects the reviews.json content",
        "Recognizes the blockquote-wrapped '**⚠️ Outside diff range comments (2)**' heading as the same kind of hidden-finding block as a Suppressed comments block, not a heading it can ignore",
        "Treats each of the two nested <details> items inside that block as its own candidate finding",
        "Confirms the categoryValues finding is real: getById's include list is missing 'categoryValues', so delete's loop at line 9 cannot be relying on the actual related rows -- the fix is adding 'categoryValues' to the include list",
        "States the second finding (delete not guarding a missing product) is stale, since delete already returns early when getById resolves to null",
        "Ends by asking the developer which findings to act on, without editing, committing, or replying to GitHub in the same turn"
      ]
    },
    {
      "id": 7,
      "prompt": "Review the unresolved comments on PR 512. Saved output of the reviewThreads GraphQL query: evals/files/pr-512-unlabeled-hidden-block/review-threads.json. Saved output of `gh api repos/<owner>/<repo>/pulls/512/reviews`: evals/files/pr-512-unlabeled-hidden-block/reviews.json. Current content of src/services/emailService.ts: evals/files/pr-512-unlabeled-hidden-block/emailService.ts. Present the findings for me to triage.",
      "expected_output": "Notices reviewThreads is empty, reads the full review body rather than checking it against a fixed list of known headings, recognizes the '<details><summary>Additional notes (1)</summary>' block as a hidden finding even though it is neither 'Suppressed comments' nor 'Outside diff range comments', and confirms the one item as a real defect (sendWelcomeEmail never awaits or catches the mailer.send call).",
      "files": [
        "evals/files/pr-512-unlabeled-hidden-block/review-threads.json",
        "evals/files/pr-512-unlabeled-hidden-block/reviews.json",
        "evals/files/pr-512-unlabeled-hidden-block/emailService.ts"
      ],
      "expectations": [
        "Does not stop at the empty reviewThreads result and report there is nothing to review — reads the full reviews.json body",
        "Recognizes the 'Additional notes (1)' heading as a hidden-finding block despite it matching neither 'Suppressed comments' nor 'Outside diff range comments'",
        "Treats the single bulleted item inside that block as its own candidate finding",
        "Confirms the finding is real: sendWelcomeEmail calls mailer.send without await or a .catch, so a rejected send becomes an unhandled promise rejection, unlike sendPasswordReset which already awaits the same call",
        "Ends by asking the developer which findings to act on, without editing, committing, or replying to GitHub in the same turn"
      ]
    }
  ]
}
